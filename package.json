{
  "name": "doc-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "develop": "cd gatsby-site && gatsby develop",
    "deploy": "cd gatsby-site && gatsby build --prefix-paths && gh-pages -d public"
  },
  "author": "Jon Arild Nygård",
  "license": "ISC",
  "dependencies": {
    "gatsby-cli": "^1.1.45"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
